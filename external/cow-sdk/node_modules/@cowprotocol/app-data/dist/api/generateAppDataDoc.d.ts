import { LatestAppDataDocVersion } from '../generatedTypes';
import { AppDataParams } from '../types';
/**
 * Creates an appData document using the latest specification of the format
 *
 * Without params creates a default minimum appData doc
 * Optionally creates metadata docs
 *
 * Example of result:
 * {
 *   "appCode": "CoW Swap",
 *   "environment": "local",
 *   "metadata": {
 *     "quote": {
 *       "slippageBips": 50
 *     },
 *     "orderClass": {
 *       "orderClass": "market"
 *     }
 *   },
 *   "version": "1.2.0"
 * }
 */
export declare function generateAppDataDoc(params?: AppDataParams): Promise<LatestAppDataDocVersion>;
