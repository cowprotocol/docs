import { AnyAppDataDocVersion } from '../generatedTypes';
/**
 * Fetches the document from IPFS using the CID
 *
 * @param cid
 * @param ipfsUri
 * @returns
 */
export declare function fetchDocFromCid(cid: string, ipfsUri?: string): Promise<AnyAppDataDocVersion>;
